<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fms.service.tuopu.ControlPropertyService">


    <insert id="addControlProperty" parameterType="com.fms.domain.tuopu.ControlProperty" >
        INSERT INTO t_control_property (controlId,propertyChinese,property,dataType,dicName,dicList,isDic,propertyFlag)
        VALUES (
        #{controlId},
        #{propertyChinese},
        #{property},
        #{dataType},
        #{dicName},
        #{dicList},
        #{isDic},
        #{propertyFlag}

        )
    </insert>

   <!-- <insert id="addCustomProperty" parameterType="com.fms.domain.tuopu.CustomProperty" >
        INSERT INTO t_custom_property (controlId,property,value)
        VALUES (
        #{controlId},
        #{property},
        #{value}

        )
    </insert>-->

<!--    <select id="getPropertyList" parameterType="map" resultType="com.fms.domain.tuopu.ControlProperty">
        SELECT * FROM t_property
    </select>

    <select id="getPropertyById" parameterType="map" resultType="com.fms.domain.tuopu.ControlProperty">
        SELECT * FROM t_property
        WHERE id = #{id}
  </select>-->

    <select id="getList" parameterType="map" resultType="com.fms.domain.tuopu.ControlProperty">
        SELECT * FROM t_control_property
  </select>

</mapper>